import { Route, Routes } from 'react-router-dom';
import Login

const index = () => {
  return (
    <Routes>
      <Route path="" element={</>}></Route>
    </Routes>
  );
};

export default index;

